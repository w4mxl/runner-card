# Apple Watch 晨跑打卡 · iOS 快捷指令配置指南

这篇指南将带你在 iPhone 上配置一个专属的“**晨跑打卡**”快捷指令。配置完成后，每天早晨跑完步：
> **在手机桌面（或锁屏/操作按钮）轻点一下** ➡️ 自动抓取 Apple Watch 刚刚记录的**时长、公里数、配速、心率** ➡️ 自动打开打卡器并填好全部数据 ➡️ 你只需选一张今早晨拍的照片，即可一键生成朋友圈/小红书大片！

---

## 5 分钟快捷指令制作步骤

打开 iPhone 自带的 **“快捷指令” (Shortcuts)** App，点击右上角 **「+」** 新建一个快捷指令，命名为 **`🏃 晨跑打卡`**。

### 第一步：获取最新跑步记录
1. 点击「添加操作」，搜索并选择 **「查找 体能训练」**（英文系统为 Find Workouts）。
2. 设置筛选条件：
   * **体能训练类型** 是 **「跑步」**（或「户外跑步」）
   * **开始日期** 是 **「今天」**
3. 展开详细设置：
   * **排序方式**：选择 **「开始日期」**
   * **顺序**：选择 **「从最新到最旧」**
   * 打开 **「限制」** 开关，将数量设为 **`1`** 项。

---

### 第二步：提取运动数据
1. 点击下方搜索框，添加操作 **「获取“体能训练”的详细信息」**：
   * 选择 **「总距离」**（确保单位为公里 km）
2. 再次添加操作 **「获取“体能训练”的详细信息」**：
   * 选择 **「持续时间」**（单位为分钟或秒）
3. 可选：再次添加操作获取 **「平均心率」** 和 **「活动千卡」**。

---

### 第三步：计算平均配速
1. 添加操作 **「计算」**：
   * 输入：`[持续时间(以秒为单位)]` ÷ `[总距离(公里)]`
   * 得到每公里耗时秒数（例如 288 秒）。
2. 添加操作 **「计算」**：
   * 计算分钟：`floor(配速秒数 ÷ 60)` -> 得到 `4` 分。
   * 计算余秒：`配速秒数 mod 60` -> 得到 `48` 秒。
   * 拼接成文本变量：`4'48"`。

> **小技巧**：如果你觉得在快捷指令里算数学公式繁琐，**打卡器本身已内置智能换算**！你只需在 URL 里传入时长和距离，打卡器会自动精准秒算配速。

---

### 第四步：带参数打开打卡器
1. 添加操作 **「URL」**，填入你的打卡器网址，并使用刚刚提取的变量拼接参数：
   ```
   http://192.168.110.211:8080/?dist=[总距离]&time=[持续时间]&pace=[配速]&hr=[平均心率]&cal=[活动千卡]
   ```
   *(注：当部署到云端如 GitHub Pages/Vercel 后，将 IP 替换为你的线上域名即可全网随时随地秒开)*
2. 添加操作 **「打开 URL」**（在 Safari 浏览器中打开）。

---

## 优雅进阶技巧

### 1. 如何直接保存到 iPhone 相册（避开“下载”文件夹）
在 Safari 网页中，点击“保存打卡图”后会弹出全屏大图预览弹窗：
* **只需长按图片 0.5 秒** ➡️ 在 iPhone 系统弹出的菜单中点击 **「存储图像」**（或「存储到“照片”」）；
* 图片会立即直接保存到 iPhone 原生照片图库的「最近项目」中，不会保存到“文件/下载”目录！

### 2. 添加到 iPhone 桌面作为独立 App (PWA)
1. 用 iPhone Safari 打开打卡器网址；
2. 点击 Safari 底部中央的 **「分享」按钮**（带箭头的方框）；
3. 向下滚动，选择 **「添加到主屏幕」**；
4. 命名为 **「跑者打卡」** 并确认；
5. 现在你的 iPhone 桌面上就会出现一个黑橙配色的极简原生 App 图标，点击后**全屏无地址栏**运行，体验如同原生 iOS App，且支持**完全离线使用**！

### 3. 绑定到 iPhone 15/16 的 Action Button（操作按钮）或轻点背面
* 进入 iPhone「设置」->「操作按钮」（或「辅助功能」->「触控」->「轻点背面」）；
* 选择快捷指令 -> 绑定 **`🏃 晨跑打卡`**；
* 晨跑拉伸结束后，长按机身侧键或双击手机背面，直接秒级呼出！
